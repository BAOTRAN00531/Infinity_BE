###############################################
#  InfinityWeb_BE - Application Configuration
#  T?t c? thông tin nh?y c?m (m?t kh?u, API key, OAuth secrets...)
#  ???c tách ra file application-secrets.properties bên ngoài d? án.
#  ???ng d?n file bí m?t này ???c set qua bi?n môi tr??ng APP_SECRETS.
###############################################

# Tên ?ng d?ng
spring.application.name=InfinityWeb_BE

# Import file secrets t? bi?n môi tr??ng APP_SECRETS
# Cú pháp: spring.config.import=optional:file:${APP_SECRETS}
#   - optional: n?u bi?n APP_SECRETS ch?a set, ?ng d?ng v?n kh?i ??ng nh?ng s? thi?u config
#   - ${APP_SECRETS}: ???ng d?n file ch?a secrets (ví d?: C:\Users\<Tên>\Documents\secrets\application-secrets.properties)
spring.config.import=optional:file:${APP_SECRETS}

###############################################
# CÁC C?U HÌNH KHÔNG NH?Y C?M (?? trong repo)
###############################################

# Hibernate Dialect cho SQL Server
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect

# Gi?i h?n upload file
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Forward headers (h? tr? proxy/reverse proxy)
server.forward-headers-strategy=framework

# B?t debug log cho Spring
logging.level.org.springframework=DEBUG


###############################################
# H??NG D?N S? D?NG
###############################################
# 1. T?o file ch?a secrets ? ngoài d? án, ví d?:
#    C:\Users\<Tên>\Documents\secrets\application-secrets.properties
#
# 2. Trong PowerShell, set bi?n môi tr??ng APP_SECRETS:
#    setx APP_SECRETS "C:\Users\<Tên>\Documents\secrets\application-secrets.properties"
#
# 3. M? terminal m?i ?? bi?n môi tr??ng có hi?u l?c.
#
# 4. Ch?y ?ng d?ng:
#    mvn spring-boot:run
#    ho?c
#    java -jar target/InfinityWeb_BE.jar
#
# L?u ý: KHÔNG commit file secrets vào Git!
###############################################
